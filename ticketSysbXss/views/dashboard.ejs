<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <h1><%= title %></h1>
    <h2>Submitted Tickets</h2>
    <ul>
        <% tickets.forEach(ticket => { %>
        <li>
            <strong>Name:</strong> <%= ticket.name %><br>
            <strong>Email:</strong> <%= ticket.email %><br>

            <!-- below is the insecure part, notice the <%- %> tag which differs from above. -->
             <!-- the %- does not escape HTML, whereas %= DOES escape html -->
            <strong>Issue:</strong> <%- ticket.issue %><br>
        </li>
        <% }) %>
    </ul>
</body>
</html>